# UDP-messages-to-ADIF-File-Integrator
This app: Listens for UDP packets on a configurable port Filters packets containing ADIF (Amateur Data Interchange Format) data Modifies the ADIF data by adding satellite-related fields Writes the processed data to an ADIF file It's designed for hamradio operators to integrate logging data from applications like Log4OM2.
